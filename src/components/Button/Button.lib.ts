export const isDisabled = (disabled: boolean): boolean => {
    return disabled;
};

export const hasLabel = (label: string): boolean => {
    return label.length > 0;  //checks if the label is not empty
};
